{
  "name": "another",
  "version": "1.0.0",
  "description": "a",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build  --win --publish always"

  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mhtahsan/another.git"
  },
  "author": "tahsan",
  "license": "ISC",
  "devDependencies": {
    "electron": "^19.0.6",
    "electron-builder": "^23.1.0"
  },
  "dependencies": {
    "electron-updater": "^5.0.5"
  }
}
